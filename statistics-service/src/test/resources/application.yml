spring:
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
    sentinel:
      enabled: false

rates:
  url: http://localhost:40888

feign:
  sentinel:
    enabled: false
