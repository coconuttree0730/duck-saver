spring:
  data.redis:
    host: localhost
    port: 6399
  cloud:
    nacos:
      discovery:
        enabled: false
      config:
        enabled: false
    sentinel:
      enabled: false

sa-token:
  token-name: satoken
  timeout: 2592000
